import React from 'react';

const AddBook = () => (
  <div>
    <div className="add-book">
      <input type="text" placeholder="Add Book" className="add-book-input" />
      <button type="submit">Add</button>
    </div>
  </div>
);

export default AddBook;
